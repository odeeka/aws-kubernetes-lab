---
apiVersion: v1
kind: Service
metadata:
  name: student-app
  namespace: student-app
spec:
  type: NodePort
  selector:
    app: student-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080